name: Call auto assign

permissions:
  contents: write
  actions: write

on:
  pull_request_target:
    types: [opened]

jobs:
  call_auto_assign:
    uses: yaronkaikov/main/.github/workflows/auto_assign.yaml@main
